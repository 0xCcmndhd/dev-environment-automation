# Example environment variables (do NOT commit your real .env to VCS)
# Copy this file to .env and fill in the values locally.

# code-server password (used if you enable code-server in the Utilities stack)
# Choose a strong passphrase; this is consumed by the code-server container.
CODE_PASSWORD=
